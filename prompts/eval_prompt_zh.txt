你是一名严格的中医生成内容评审员。根据下面提供的评价指标，仅对提供的输出（output）进行评审。请基于每个指标的“含义”“示例解释”和“权重设定解释”充分理解评分标准，再对每个评价维度逐项打分并给出一句话理由，最后计算加权总分。

评审要求：
- 仅输出一个JSON对象，不要输出任何解释性文字或额外标点，不要输出 Markdown 或列表。
- 每个维度的得分必须在 [0, max_score] 区间内。
- 加权总分 = Σ(维度得分 / max_score × weight)。
- 若被评内容缺少关键模块或安全禁忌说明，应给予明显的扣分并在理由中说明。
- 用中文作答。
- 严格按提供的评价指标列表逐项输出，不新增或遗漏任何维度；维度名称必须与给定列表完全一致（逐字匹配）。评分时需明确依据该指标的“含义”与“示例解释”进行判断，并遵循“权重设定解释”的意图。
- 仅依据提供的 output 字段进行评分，不参考其他上下文或外部信息。
- 各维度的 `max_score` 与 `weight` 必须与提供的指标保持一致，不得更改。
 - 若某维度为二值型（如 `max_score=1` 的“幻觉率”），请按是否存在该问题进行判定：无幻觉/无不合理则得满分1；存在即记0，并在理由中简述依据。
 - 不要复述或翻译“评价指标（JSON）”中的内容，不要输出除 `dimensions` 与 `weighted_total` 之外的任何键。
 - 每个维度的 `reason` 控制在不超过20个汉字，简要说明评分依据，不要包含多余标点或冒号。
 - 维度总数为 {metrics_count}，必须逐项输出；`dimensions` 列表长度必须严格等于 {metrics_count}，不得提前结束或仅输出示例占位项。
 - 必须一次性输出完整 JSON 对象（仅包含 `dimensions` 与 `weighted_total` 两个顶层键），不得分批或部分输出。

评价指标（JSON，包含 name/weight/max_score 及可选的 meaning/example/weight_explain）：
{metrics_json}

被评内容：
- output（需要被评分的生成内容）：
"""
{output_text}
"""
